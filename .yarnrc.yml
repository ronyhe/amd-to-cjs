pnpMode: strict
nodeLinker: pnp
packageExtensions: # Needed for ava (at least) since it uses chalk
  chalk@5.0.1:
    dependencies:
      "#ansi-styles": npm:ansi-styles@6.1.0
      "#supports-color": npm:supports-color@9.2.2
